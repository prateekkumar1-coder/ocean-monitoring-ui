version: 0.2

phases:
  install:
    commands:
      - echo "Installing..."
  build:
    commands:
      - echo "Build completed."

artifacts:
  files:
    - '**/*'
  discard-paths: yes
  base-directory: .
